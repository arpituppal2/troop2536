# Optional: only used for local preview (`bundle exec jekyll serve`).
# GitHub Pages builds the site itself with its own github-pages gem;
# this pin simply matches that environment locally.
source "https://rubygems.org"

gem "github-pages", "~> 232", group: :jekyll_plugins
gem "webrick"
